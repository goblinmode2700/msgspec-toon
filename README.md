# msgspec-toon

A native TOON 4.1 codec for Python with a direct typed path into `msgspec.Struct`.
See `openspec/specs/` for the authoritative requirements and
`docs/implementation-spec/` for the engineering design of record.

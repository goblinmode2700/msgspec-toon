{
  "baseline": {
    "tag": "v0.1.0-conformant",
    "harness": "benches/ab.py \u2014 alternating B C C B blocks in one session on one machine; the reported delta is the median of the paired ratios, and the noise floor is the spread among repeated blocks of the SAME build. Figures before F-12 came from a single B-then-C run, which could not separate a small delta from run-order drift",
    "instrumentation": "the frozen v0.1.0-conformant wheel carries the alloc-stats counters in release; they became test-only at F-05. ab.py records baseline_instrumented/current_instrumented in ab-latest.json so a reader can see which side had them",
    "estimator_note": "Every figure recorded before the trustworthy-performance-evidence change was produced by the min-of-batches estimator: the minimum per-call time across 7 autoranged batches in one process. That estimator rewards whichever batch dodged the scheduler and has no central-limit behavior to converge on. Figures under it and under the mean-across-workers estimator that replaced it are NOT comparable, and no historical figure is restated under the new estimator without re-measurement.",
    "roles": {
      "story": "v0.1.0-conformant (.venv-baseline) \u2014 what the optimization round bought. Reported, never gated: every figure in `candidates` below is measured against it",
      "guard": "v0.2.0 (.venv-guard) \u2014 the latest release, and the only thing `make ab` gates against. Re-cut it at every release or it decays into the same blind spot"
    }
  },
  "candidates": [
    {
      "id": "E1",
      "target": "encode scalar dispatch",
      "hypothesis": "exact-type pointer compares beat a chain of limited-API type-check calls per cell",
      "status": "adopted",
      "commit": "perf: E1 type-pointer dispatch + E2 row capacity reservation",
      "ab_delta": {
        "typed_encode": [
          "-4.6% @16",
          "-2.2% @64",
          "-4.5% @512",
          "-5.3% @4096"
        ],
        "untyped_encode": [
          "-7.9% @16",
          "-5.2% @64",
          "-5.9% @512",
          "-6.5% @4096"
        ]
      },
      "note": "measured together with E2; also recovered the +14-15% typed-encode regression that the wire-options plumbing introduced (caught by the A/B harness on its first run)",
      "requalified_under_f12": "the encode gain is resolved at 512 and 4096 records; at 16 and 64 it is smaller than the session noise floor and is reported as unresolved rather than as a win",
      "estimator": "min-of-batches"
    },
    {
      "id": "E2",
      "target": "encode output buffer",
      "hypothesis": "one up-front capacity reservation for tabular rows removes growth-doubling copies",
      "status": "adopted",
      "commit": "perf: E1 type-pointer dispatch + E2 row capacity reservation",
      "ab_delta": {
        "combined_with": "E1"
      },
      "requalified_under_f12": "the encode gain is resolved at 512 and 4096 records; at 16 and 64 it is smaller than the session noise floor and is reported as unresolved rather than as a win",
      "estimator": "min-of-batches"
    },
    {
      "id": "P1",
      "target": "row cell splitting",
      "hypothesis": "quote-free rows (the overwhelming majority) can split on a plain memchr scan instead of byte-at-a-time quote tracking",
      "status": "adopted",
      "commit": "perf: decode candidates P1+D2+D3+D1 adopted",
      "ab_delta": {
        "combined_with": "D1+D2+D3, see D1 row"
      },
      "estimator": "min-of-batches"
    },
    {
      "id": "D2",
      "target": "decode row buffers",
      "hypothesis": "reusing one cells buffer across rows removes an allocation per row",
      "status": "adopted",
      "commit": "perf: decode candidates P1+D2+D3+D1 adopted",
      "ab_delta": {
        "combined_with": "D1+P1+D3, see D1 row"
      },
      "estimator": "min-of-batches"
    },
    {
      "id": "D3",
      "target": "untyped decode key strings",
      "hypothesis": "tabular rows repeat the same few keys; one cached PyString per column beats one fresh PyString per cell row (O(rows*cols) -> O(cols) allocations)",
      "status": "adopted",
      "commit": "perf: decode candidates P1+D2+D3+D1 adopted",
      "ab_delta": {
        "untyped_decode": [
          "-10.5% @16",
          "-16.0% @64",
          "-21.0% @512",
          "-18.4% @4096"
        ]
      },
      "note": "this also sped up the G3 wrapper baseline (which uses our untyped decoder), thinning typed decode's margin at 64 records to noise level in one run \u2014 resolved by implementing D1's typed-side memo rather than rejecting a genuine improvement; G3 re-verified passing at every size afterwards",
      "estimator": "min-of-batches"
    },
    {
      "id": "D1",
      "target": "typed decode field resolution",
      "hypothesis": "rows of one array repeat an identical key-event sequence; record the first row's wire-name->index resolutions and replay them positionally (byte compare instead of hash per cell), with FxHash replacing SipHash for the fallback map",
      "status": "adopted",
      "commit": "perf: decode candidates P1+D2+D3+D1 adopted",
      "ab_delta": {
        "typed_decode": [
          "-14.7% @16",
          "-20.8% @64",
          "-17.3% @512",
          "-23.6% @4096"
        ]
      },
      "estimator": "min-of-batches"
    },
    {
      "id": "D4",
      "target": "decode scalar UTF-8 revalidation",
      "hypothesis": "borrowed scalar slices need no per-cell UTF-8 revalidation after document-level validation",
      "status": "already-implemented-at-baseline",
      "note": "pyval.rs used unchecked conversions from the first implementation; no separate measurement possible"
    },
    {
      "id": "E3",
      "target": "fused per-row byte templates",
      "hypothesis": "precomputed indent+delimiter templates per shape reduce writer calls per row",
      "status": "not-attempted",
      "note": "deferred: remaining encode gap vs to_builtins is getattr-bound (R-02), and E1/E2 already recovered the options regression; candidate remains open with its hypothesis intact"
    },
    {
      "id": "G4x",
      "target": "whole typed encode vs msgspec.to_builtins alone",
      "hypothesis": "E1-E3 together might close G4 at large payloads",
      "status": "measured-outcome",
      "note": "G4 still fails: 2.36us vs 1.05us at 16 records, 494us vs 447us (~10% gap) at 4096. The floor remains public stable-ABI attribute reads vs msgspec's private C slot reads (canvas risk R-02). Reported, not masked."
    },
    {
      "id": "D6",
      "target": "strict-mode duplicate-key detection in the parser",
      "hypothesis": "note_key kept seen keys in a Vec and scanned it linearly, so an object with N keys paid O(N^2) byte comparisons; keyed tabular bodies make N the row count. An FxHashSet should turn that into O(N) and be invisible everywhere else.",
      "status": "adopted",
      "source": "external review round (Fable 5), patch 01 of 5",
      "measurement": "make ab vs the v0.3.0 guard: keyed decode -18.4% at 64, -50.7% at 512, -88.0% at 4096 (MDE 0.9-1.4%); 16 and every non-keyed metric resolved no significant difference. The growth with size is the quadratic signature the hypothesis predicted.",
      "note": "The candidate arrived with its own gate-coverage warning and it was correct: the ladder had no keyed payload, so every existing metric reads flat and D6 would have been rejected as noise. benches/payloads.py gained keyed_document/keyed_toon_text and ab.py a 'keyed decode' metric BEFORE this was measured. Rejecting a real 88% win for want of a payload is the same failure as the speed gate that could not see a 24% regression."
    },
    {
      "id": "P2",
      "target": "bare scalar classification (src/scalar.rs)",
      "hypothesis": "classify_bare ran an integer scan and then re-scanned the same bytes through the float grammar, so every bare string paid two failed passes. One pass over the number grammar should win on every decode, and on encode too while needs_quote still calls classify_bare.",
      "status": "adopted",
      "source": "external review round (Fable 5), patch 02 of 5",
      "measurement": "make ab vs the v0.3.0 guard, cumulative with D6; P2's share isolated against the D6-only run: typed decode -2.5 to -4.5%, untyped decode -3.3 to -3.5%, typed encode -6.7% at 4096. The encode win is expected at this point in the stack because needs_quote still routes through classify_bare; E5 later removes that call.",
      "note": "Verified by an exhaustive differential against the replaced two-pass implementation, kept in src/scalar.rs as a #[cfg(test)] oracle: every token up to length 5 over the alphabet that decides the grammar (0192.eE+-n) plus the edge literals, 111,126 tokens, zero divergences."
    },
    {
      "id": "E5",
      "target": "quoting decision and string escaping (src/encode.rs)",
      "hypothesis": "needs_quote made up to four passes over the same bytes (classify_bare, numeric-like, forbidden-byte, prefix checks) and write_quoted re-encoded every character through UTF-8. One pass for the decision and bulk span copies for the escaping should win on every string cell.",
      "status": "adopted",
      "source": "external review round (Fable 5), patch 03 of 5",
      "measurement": "make ab vs the v0.3.0 guard, cumulative; E5's share isolated against the P2 run: typed encode -13.3 to -17.2% cumulative (E5 adds ~-10 points over P2), untyped encode -6.1 to -9.0%. Decode is unchanged by E5, as expected.",
      "note": "The pass-collapsing rests on one observation: every valid number token is also numeric-like, so the number grammar needs no separate pass in the quoting decision. Verified by differential against the replaced multi-pass implementation, kept as a #[cfg(test)] oracle: 107,841 (string, delimiter) pairs over an alphabet covering delimiters, structural bytes, controls, the number grammar and multi-byte UTF-8, plus 35,937 escape comparisons. Zero divergences. The efficiency lock independently confirms canonical bytes did not move."
    }
  ],
  "token_findings": {
    "T1": "PASS \u2014 canonical TOON is 0.61-0.64x compact JSON tokens (o200k_base) on the uniform-record ladder; 0.80x on string-heavy; 0.65x on numeric-heavy",
    "incumbents": "both v3.0-era codecs cost 1.25x JSON tokens on the challenge shape \u2014 worse than the format they replace",
    "T2": "the tab-delimiter folklore does not hold under o200k_base: differences are within a few tokens per ten thousand either way (tab wins marginally on numeric-heavy, loses one header token elsewhere); losing points published per the requirement"
  },
  "measurements": {
    "instrumentation_cost": {
      "question": "what did the release-wheel G2 counters cost the sides that carried them?",
      "method": "benches/ab.py --baseline-venv .venv-g2 \u2014 instrumented vs clean, same commit, same session",
      "result": "not resolvable by this harness: every row placed the CLEAN build 0.4-3.6% slower, which cannot be a real effect of removing work",
      "conclusion": "the counters were a proof-quality defect, not a measurable speed defect; the published G3/G5 margins were not materially inflated by them",
      "artifact": "benches/ab-instrumentation-cost.json",
      "follow_on": "the sign-consistent penalty on the second-run side independently reproduces F-12: baseline always runs first, and run order is worth 1-3% \u2014 larger than the effect under test"
    },
    "run_order_drift": {
      "question": "how much of a published delta is the machine rather than the code?",
      "method": "benches/ab.py --rounds 2 \u2014 eight blocks, B C C B twice; noise floor is the spread among the four blocks of each build",
      "result": "same-build blocks disagree by 1.7-8.8 percentage points depending on metric and size; an earlier single-order run put whichever side ran second 0.4-3.6% slower on every row",
      "consequence": "decode deltas (-13 to -20%) sit well above the floor and are resolved; typed-encode at 16 and 64 records (-5.4%, -4.2%) sits BELOW it and is not a result on this machine",
      "action": "ab.py now labels below-noise rows explicitly and keeps every block in benches/ab-latest.json"
    },
    "worker_count_choice": {
      "question": "how many worker processes does the mean-across-workers estimator need?",
      "method": "bench_typed at 16 records with 8 workers; read the per-worker coefficient of variation, then compute the standard error of the published mean as CV/sqrt(workers)",
      "result": "per-worker CV on a quiet machine: typed decode 0.80%, typed encode 1.43%, to_builtins 1.13%, wrapper decode 1.18%. Standard error at 10 workers: 0.25-0.45%",
      "choice": "10 workers. The smallest count whose standard error sits well under the >=4% effects this project reports; 25 workers would buy about 0.15 percentage points for 2.5x the runtime",
      "caveat": "per-worker CV is a property of machine load, not of the codec: the same metric measured 7.84% CV during a heavily loaded moment in the same session. The standard error above is a quiet-machine figure",
      "estimator": "mean-across-workers"
    },
    "ab_false_positive_check": {
      "question": "does the significance gate stay quiet when nothing changed?",
      "method": "benches/ab.py --baseline-venv .venv \u2014 the current build against itself, 8 metrics",
      "result": "first pass: 7 of 8 quiet, and typed encode@512 reported +1.6% SLOWER with a 1.4% minimum detectable effect \u2014 a false positive, as expected from 8 tests at alpha 0.95",
      "consequence": "a slowdown now triggers an independent confirmation run and only a reproduced slowdown fails the gate; re-run returned 8 of 8 no-significant-difference and exit 0",
      "estimator": "mean-across-workers"
    },
    "gate_reference_point": {
      "question": "can the speed gate detect a regression at all?",
      "method": "inject a heap allocation per placed value into the typed decode path (139 -> 172 us, a 24% slowdown), then run the gate",
      "result": "against the STORY baseline the same 24% regression read as +2.2%, verdict 'no significant difference', exit 0 \u2014 the current build led that baseline by 15-20%, so the regression only erased the lead. Against the GUARD baseline it read +24.4%, reproduced on the confirmation run, exit 1",
      "consequence": "the gate measures against the latest release; the frozen tag stays as the reported story. A first attempt at a slowdown (black_box over 40 additions) compiled away to 137.67 us vs 139.03 clean, and the gate correctly stayed quiet",
      "estimator": "mean-across-workers"
    },
    "open_question_encode_4096": {
      "question": "is there a small typed-encode regression at 4096 records against v0.2.0?",
      "status": "RESOLVED \u2014 yes, and it is real",
      "evidence": "16 blocks per side, one metric per block, guard CV 1.08% and current CV 3.64%: +2.42% with a 1.98% minimum detectable effect, significant. Earlier runs that could not resolve it were underpowered (2-3 rounds) or contended (one had 40.8% CV on the current side, and the t-test correctly refused to call a +29.8% reading)",
      "attribution": "reverting ONLY the two encode-path changes since v0.2.0 (the build_shape depth guard and the capped writer reservation) moved it to +1.38%, still significant. Both run about five times per encode of 4096 records, so neither can plausibly cost 1% of a 500us call; the residual and probably the whole effect is binary layout and inlining changed by the new modules, which is a known measurement phenomenon rather than a code defect",
      "next": "belongs to the profiling work (Phase E), not to a revert: the logic is O(1) per array and the corpus, containment tests, and G2 all depend on it",
      "estimator": "mean-across-workers"
    },
    "confirmation_power": {
      "question": "is a same-power confirmation run enough to reject a false positive?",
      "result": "no. typed encode@4096 flagged at +2.5% and its same-power confirmation reproduced it; a 3-round run then read +2.1% with a 2.6% MDE, i.e. not significant",
      "consequence": "confirmation now runs at double the block count, so it has more power than the test that raised the flag rather than the same",
      "estimator": "mean-across-workers"
    },
    "ab_block_cost": {
      "question": "why did a gate run take ten minutes?",
      "cause": "each A/B block called a sampler that measured every metric in the module and reported one, so every typed-encode block also timed the python-toon incumbent at ~30ms a call",
      "fix": "MSGSPEC_TOON_ONLY_METRIC makes the sampler skip metrics the block will discard; a 4096 typed-encode block fell from about 4s to 0.5s",
      "caveat": "the sampler metric name is now listed in the METRICS table rather than derived from the result key \u2014 deriving it silently mismatched half the metrics, and a block that measures nothing reports a mean of zero"
    }
  }
}
